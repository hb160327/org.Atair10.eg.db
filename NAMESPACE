import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    org.Atair10.eg,
    org.Atair10.eg_dbconn,
    org.Atair10.eg_dbfile,
    org.Atair10.eg_dbschema,
    org.Atair10.eg_dbInfo,

    org.Atair10.egORGANISM
)

